import React from 'react';

const Hex = () => {
    return (
        <section className="hex">
            <p>{`Hex Value: ${this.props.hexValue}`}</p>
        </section>
    );
};

export default Hex;